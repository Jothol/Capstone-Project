<LS_Tab3>
    Image:
        id: background_image
        source: '../other/images/temp_logo.png'
        fit_mode: 'scale-down'
        z: -1

    # Button:
        # id: add_button
        # size_hint: (None, None)
        # size: (130, 30)
        # text: 'Add User'
        # pos_hint: {'x': 0.01, 'y': 0.08}
        # on_press:
            # root.open_add_account()

    # Button:
        # id: remove_button
        # size_hint: (None, None)
        # size: (130, 30)
        # text: 'Remove User'
        # pos_hint: {'x': 0.21, 'y': 0.08}
        # on_press:
            # root.open_remove_account()
