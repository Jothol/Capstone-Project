<LS_Tab1>
    add_button:add_button.__self__
    Button:
        id: add_button
        size_hint: (None, None)
        size: (130, 30)
        text: 'Connect to Chat'
        pos_hint: {'x': 0.01, 'y': 0.08}
        on_press:
            root.connect()